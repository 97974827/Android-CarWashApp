<resources>
    <!-- Base application theme. -->
<!--    <color name="colorPrimary">#e0e0e0</color>-->

    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>

        <!--Status Bar 글자색깔-->
        <item name="android:windowLightStatusBar">true</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <!-- 장비주소 커스텀 디자인 -->
    <style name="CustomDeviceAddrStyle" parent="@android:style/Widget.TextView">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:textColor">#1abc9c</item>
        <item name="android:layout_marginLeft">78dp</item>
        <item name="android:textSize">30sp</item>
        <!--<item name="android:gravity">center</item>-->
    </style>

    <!-- 장비명 커스텀 디자인 -->
    <style name="CustomDeviceNameStyle" parent="@android:style/Widget.TextView">
        <item name="android:layout_width">70dp</item>
        <item name="android:layout_height">40dp</item>
        <item name="android:gravity">center</item>
        <item name="android:layout_marginTop">30dp</item>
        <item name="android:background">@drawable/button_round</item>
        <item name="android:textColor">#ffffff</item>
        <item name="android:textSize">30sp</item>
    </style>

    <!-- 오늘의 매출 -->
    <style name="CustomTodaySalesStyle" parent="@android:style/Widget.TextView">
        <item name="android:layout_width">70dp</item>
        <item name="android:layout_height">40dp</item>
        <item name="android:layout_gravity">center</item>
        <item name="android:layout_marginTop">30dp</item>
        <item name="android:background">@drawable/textview_sales</item>
        <item name="android:textColor">#ffffff</item>
        <item name="android:textSize">30sp</item>
    </style>

</resources>