<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.gls.carwashapp">

    <!-- 인터넷 사용을 위한 추가 -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar">
        <activity android:name="com.gls.carwashapp.activity.DaySalesActivity"></activity>
        <activity android:name="com.gls.carwashapp.activity.MonthSalesActivity" />
        <activity android:name="com.gls.carwashapp.activity.SelectRealActivity" />
        <activity android:name="com.gls.carwashapp.activity.RealActivity" />
        <activity android:name="com.gls.carwashapp.activity.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> <!-- 새로운 액티비티를 만들어 앱에 추가시 새 액티비티 정보가 추가됨 -->
        <activity android:name="com.gls.carwashapp.activity.MainActivity" />
    </application>

</manifest>